# Web server — no ML dependencies here. All real-time inference runs
# client-side in webapp/frontend/; this process only serves JSON/images and
# logs telemetry.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Tests
pytest>=7.4.0
httpx>=0.27.0
